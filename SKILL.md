---
name: boss-writing
description: 老板级和跨团队中文材料写作与改写规范。用于季度规划、区域汇报、项目复盘、周报、日报、异动简报、立项、PPT 母稿、演讲稿和管理层简报。按材料类型提供三种叙事骨架（介绍一件事 / 报告变化 / 复盘）。用户说“写汇报、写材料、改写、精简、说人话、去 AI 味、给老板看、给区域讲、语气有攻击性”时使用。核心是保留成熟原文、先业务后工具、结论先行、客观不归责、专业且能讲出口，并通过业务词包、作者档案与反馈事件持续学习写作偏好。
---

# 老板级材料写作（boss-writing）

> **公开版 v1.0**。萃取自多轮真实管理层材料的评审反馈，已去除具体业务内容，文中示例均为虚构。
> 定位：**管文风**（句子怎么写），不管取数与发布流程。
> 一句话诊断：多数“太啰嗦”的材料是**术语病，不是结构病**——先治句子里的词，别推倒骨架。

## P0、先守住原稿，再开始改写

> 起因：一版材料的成熟表达被整体重写后，虽然更短，却出现「让好经验不再只留在个人手里」「区域愿不愿用」等带归责感的句子。结构变清楚了，听感反而变差。

**改写不是重新创作。** 用户要求“优化、精简、说人话”时，默认保留已经成熟的观点句、开场总结、带走句和自然转场。除非用户明确要求换风格，不要为了显示改动而改写。

动笔前先建立三类清单：

1. **原样保留**：用户认可过的句子；准确、自然、可直接讲出口的句子；已经成为全文主线的判断。
2. **局部调整**：事实正确但过长、重复或含少量术语的句子。只改问题所在，不整句推倒。
3. **结构移动或删除**：重复信息、章节职责错位、偏离主线的内容。移动内容不等于重写内容。

默认改写合同：

> **结构可以重排，成熟表达尽量不动；删除必须有重复或偏题依据，改写必须能指出原句的问题。**

改完必须做逐句对照：新句如果没有比原句更准确、更清楚、更温和，就恢复原句。

## P0·五、听感与归责闸门：问题对事，不对人

汇报材料不仅要“逻辑正确”，还要让听众愿意继续合作。特别是中台对区域汇报，默认关系是并肩解决问题，不是评价、教育或验收区域。

### 三类攻击性来源

| 来源 | 有刺写法 | 温和写法 | 问题 |
|---|---|---|---|
| 暗示个人不分享 | 让好经验不再只留在个人手里 | 让经验可以复制 | 把机制问题写成个人意愿问题 |
| 给听众下判断 | 看区域愿不愿用 | 验证工具是否适合真实业务 | 把产品验证写成考核区域 |
| 用否定标签概括现状 | 好方法复制不了 | 好的做法难以稳定复制 | 绝对化、压迫感强 |
| 居高临下要求 | 区域要把问题提出来 | 希望和区域一起发现问题、优化方法 | 中台站到了听众对面 |

### 攻击性自检

逐页追问：

1. 这句话把问题归因给了流程、信息和工具，还是归因给了某一群人？
2. 听众听到后会觉得“这在描述共同面对的困难”，还是“这在评价我做得不好”？
3. 删掉主语后，是否仍在暗示“不愿分享、不愿使用、不会做、做得落后”？
4. 是否滥用“让……不再”“必须”“愿不愿”“只停留在”“复制不了”等带命令或审判感的结构？

命中后不要只换同义词，要改写问题归因：

- 个人经验难保存 → **好的做法难以稳定复制**
- 区域愿不愿用 → **工具是否适合真实业务使用**
- 一线只靠个人判断 → **现有判断标准分散，难以保存和复盘**

**例外**：事故复盘、责任认定等明确要求指出责任的材料，按事实写清责任；不要用“温和”掩盖事实。

## P0·七、开口讲之前做三遍听感检查

1. **原文对照读**：逐句比较改前改后。新句只是更短、更新鲜，不算更好。
2. **站在听众位置读**：把自己当成区域、一线或被汇报团队，标出任何像批评、命令、考核的句子。
3. **朗读**：现实会议里不会这样说，或者说出口让人不舒服，就不要写。

判断优先级：

> **事实准确 > 关系不受伤 > 一次听懂 > 表达有力 > 形式整齐。**

对仗、短句、标题观点化都只是手段；与听感冲突时，优先保护事实和关系。

## P0·八、介绍工具必须讲“任务怎么变”，不能只讲功能

> 起因：一版材料用「服务区域和一线团队，看清周边机会，为客户拜访提供数据支持」介绍工具。句子没有错，但听众听完仍不知道自己具体少做了什么、接下来怎么用，等于没有介绍。

**“服务谁 + 看清什么 + 提供支持”不是业务价值，只是产品简介。** 面向区域介绍工具，必须讲清一次真实任务在使用前后发生了什么变化。

动笔前回答五个问题：

1. **以前怎么做**：需要搜哪些地方、拼几类信息、反复核对什么？
2. **麻烦在哪里**：时间花在哪、哪些结果难更新、哪些过程看不见？
3. **现在怎么做**：打开什么、选择什么、看到什么、保存什么？
4. **留下什么**：名单、地图、编号、问题记录还是过程数据？
5. **管理上得到什么**：能否识别表现好坏、及时响应问题、调整下一轮方法？

默认写法：

> **以前怎么完成任务 → 现在怎么完成任务 → 使用者因此少做什么、能多看见什么**

### 反例：抽象价值

> 选址推荐服务区域和一线团队，提高选点、开区和拓展效率。

听完不知道效率提高在哪里。

### 正例：把任务前后讲出来

> 以前需要逐个搜索、反复核对，再把结果拼进多个表格。现在按标签批量圈选，在地图上集中判断，名单保存后还能继续跟踪和回溯。区域少做重复整理，把时间放在判断名单和推动行动上。

### 反例：对象罗列

> 客户洞察工具服务客户、重点客户和区域团队，看清周边机会，为客户拜访提供数据支持。

听完不知道拜访时多说了哪一句有用的话。

### 正例：把使用场景讲出来

> 以前拜访客户时，很难回答“周边有哪些竞争对手”。现在可以快速说明竞争对手、客群分流和经营机会，让区域带着围绕这个客户的分析去沟通。

### 试点验证也要站在使用者视角

对区域讲试点，少讲“产品可用、业务可执行、结果可回流”。这些是中台验收语言。改讲区域能感受到的四件事：

1. 名单准不准；
2. 使用省不省力；
3. 执行过程看不看得见；
4. 发现的问题能不能及时记录、响应并进入下一轮调整。

最终落点不是“证明产品成立”，而是让区域少做重复劳动，及时看见过程中的好坏，并把一次试用变成可以持续改进的方法。

## 一、八条核心方法（冲突时以此为准）

1. **标题即观点**：每个标题是一个可单独摘出的判断，不是栏目名。「从资源覆盖转向有效经营空间」✓；「CHAPTER 03 · SOLUTION」✗。
2. **「不是A而是B」**：先破掉读者默认的误解，再给更准的判断。「不是缺点位，而是缺'值得去的点位'」。全文最强武器，改写时优先保留。
3. **数字带解读**：每个关键数字后跟“这说明/所以”。「覆盖率 12%，还有 8000 多个空白点位——所以问题不是没地方可去」。孤立数字卡 = 不合格。
4. **诚实分层**：区分“有”和“能不能真正用”（覆盖≠可运营、上线≠好用、试点≠推广）。主动承认边界比满格口号更可信。
5. **落到可执行动词**：每个能力讲「谁 · 看什么 · 做什么」。名词化口号（引擎/飞轮/赋能/底座）→ 动作。
6. **框架锁死贯穿**：一套 4-5 词的主框架全篇用完全相同的词，不换同义词。
7. **带走句**：每章/每页收敛一句加粗核心句，位置固定。
8. **总分总、先结论**：每篇、每段先抛业务结论，再给支撑。工作是论据不是主角——“接了数据源、建了表、跑了匹配…所以落后了” = 反面教材。

## 二、先选骨架：你在写的是哪一种材料

**同一套句子规则，三种叙事骨架。选错骨架，句子写得再好也别扭。**

| 你在写什么 | 走哪条 | 典型体裁 | 频率 |
|---|---|---|---|
| 介绍**我们做的一件事** | §二·五 **四段论证**<br>解决什么问题 → 提供什么 → 相对优势 → 当前进展 | 方案、能力介绍、立项、产品说明 | 季度级 |
| 报告**发生了什么变化** | §二·六 **F-I-I-J**<br>事实 → 为什么重要 → 对业务的影响 → 我的判断 | 日报、周报、异动简报、数据播报 | **日 / 周级** |
| 复盘**一段已经完成的工作** | 下方 **业务闭环五步**<br>现状 → 问题 → 机会 → 行动 → 效果 | 季度复盘、项目结项、QBR | 季度级 |

**不确定选哪个，问一句**：这份材料是在回答「这是什么」，还是「发生了什么」，还是「结果如何」？

⚠️ **骨架管的是段落怎么组织，不管句子怎么写。** 八条核心方法（§一）、去术语（§三）、
短句化（§四）、表达红线（§七）、事实纪律（§七·五）**对三种骨架一视同仁**，不因换骨架而放松。

### 业务闭环五步（复盘型材料）

- **业务闭环五步**：现状（怎么样了）→ 问题（发现了什么）→ 机会 → 行动（要拍的单列 ASK）→ 效果（回收验证）。⑤ 又变成下轮 ①——让老板看到“上次说的这次回收了没”。顺序不能乱：永远先讲业务状态和判断，再讲支撑动作。
- **详略按战略重要性分配**：主菜板块展开到底，次要一笔带过。结构对但详略失衡照样失败。
- **每个发现就近配一个可考核的动作**，不攒到最后统一给。

## 二·二、先确定听众：同一句话换对象，重点就变

每一页都要回答听众的一个真实问题：

- 为什么要做？这和我有什么关系？
- 你们具体提供什么？现在能不能用？
- 用了有什么帮助？还没有验证什么？
- 需要我做什么？你们会提供什么支持？

一句话只是在表达“我们很努力”，却没有回答以上问题，删除或重写。

### 对区域和一线汇报

先保护合作关系，再讲问题。重点讲实际场景、怎么使用、中台提供什么支持。少用“验收、要求、必须、愿不愿”等上对下词汇。

默认链路：

> **业务问题 → 中台提供的名单/图/工具 → 区域怎么使用 → 反馈如何帮助下一轮优化**

### 对老板和管理层汇报

重点讲判断、投入产出、边界、取舍和需要拍板的事项。问题可以更尖锐，但事实与责任必须有证据。

### 每页最小结构：结论 + 最多 3 条论据

1. 一个能单独成立的观点标题。
2. 一句结论。
3. **最多 3 条论据 —— 这是上限，不是目标。**
4. 一个必要证据或当前边界。
5. 一句自然转场或下一步。

**为什么卡在 3 条**：不是排版考虑，是**论据超过三条会互相稀释**——读者记不住第四条，
反而把前三条也冲淡了。两条足够就写两条，一条说得清就写一条。

**凑数的代价是可见的**：为了填满而加进来的论据，通常是「开了个会」「调了个字段」这类
工作过程，它们一进来，这一页就从「帮人做判断」退回「汇报劳动」。

演讲型材料优先“能讲出口”，不要把母稿里的全部解释都塞进页面。

## 二·五、回归本质：四段论证（讲一件事的默认结构）

> 来源：一次季度材料重写后被评为“最喜欢的版本”，随后要求推广到全局。
> 适用：任何“我们做了 / 要做一件事”的段落——一章、一个能力、一个模块都能套。

**病根**：讲一件事时，人的默认写法是“讲我们怎么做的”（判据标准化 → 策略可复现 → 有效性验证）。
这是**产品经理视角**，听的人不关心。要改成**讲这件事解决了什么**。

| 段 | 写什么 | 硬要求 |
|---|---|---|
| **一 · 解决什么问题** | 谁、在什么情况下、遇到什么具体困难 | 描述那个场景，**不许写“痛点”二字**；不许指责执行方 |
| **二 · 我们提供什么** | 具体的交付物 | 名单 / 图 / 规则 / 接口 / 报告——**不是“能力”“方案”“体系”** |
| **三 · 相对现状的优势** | 跟“现在怎么做”比，好在哪 | **必须有对照物**。没有对照的“优势”是自说自话 |
| **四 · 当前进展** | 到哪一步了 | 如实说。快照就说快照，验证过一次就说一次 |

**反面对照**（同一件事的两种写法）：

> ❌ 「同一批数据换个主角，就是能直接拿去谈的工具。现在的产品都在回答『我的店怎么样』，把视角换成『谁在跟我抢』，同一批数据就成了见客户时能直接打开的东西。」
> —— 讲的是“我们怎么想的”，且口语化。评审判定：太低端。

> ✅ 「**解决什么问题**：客户在区域市场中长期处于竞争信息盲区。一家门店的经营者通常只掌握自己店的数据，对周边真实的客流争夺格局缺乏判断依据——谁在分流他的客户、分流了多少、还有哪些区域未被覆盖，过去没有可靠来源。直接后果是平台与客户的沟通停留在单向陈述：我方讲流量规模，客户讲经营压力，双方缺少共同认可的事实基础。
> **我们提供什么** / **相对优势** / **当前进展**……」

**判断自己写没写对**：第一段读完，听的人应该能复述出“谁遇到了什么麻烦”。复述不出来 = 还在讲自己。

## 二·六、F-I-I-J：报告一个变化的默认结构

> §二·五 回答「我们做的这件事是什么」，本节回答「**发生了什么变化**」。
> 日报、周报、异动简报走这条。

**内核是一次责任转移：把判断的劳动从读者身上，挪到作者身上。**

「今天做了什么」是把原始材料倒给读者，让他自己判断重不重要、关不关自己的事、该不该动——
认知劳动全在读者。F-I-I-J 是作者替读者把这四步做完。

| 层 | 回答 | 常见写错 |
|---|---|---|
| **F 事实** | 发生了什么 | 混进解读；或只有数字没有口径 |
| **I 为什么重要** | 这件事对**这个领域**意味着什么 | 写成"很重要""值得关注"这类空话 |
| **I 对业务的影响** | 对**你，读者**有什么后果 | 与上一层重复，见下方区分判据 |
| **J 我的判断** | 所以该怎么看、怎么办 | 写成"要继续努力"这类没排除任何选项的话 |

### 它是检查表，不是版式

⛔ **不要把这四个词当小标题印在页面上。** 那就成了填空，正踩 §七 表达红线里
「SMART 是检查表不是句式」那一条，也过不了 §P0·七 的朗读检查——现实会议里没人这么说话。

**正确用法是写完自检**：这条有没有事实？有没有说清为什么值得看？有没有讲对谁有影响？我有没有亮出判断？
四问都有，四层就自然长在叙事里了。

### 三条硬约束

**① 判断必须可证伪。** 判据很硬：**一个判断如果没有排除掉任何选项，它就不是判断。**

- ❌「下一步要继续优化产品体验」——什么都没排除
- ✅「现在不该继续加字段，该先把『找到之后怎么办』补上」——排除了加字段

**② 判断要让人看出是判断。** 这是与 §七·五 事实纪律的边界：那一节禁止把没核过的东西
当事实写，本节要求你亮出观点——两者不冲突，前提是**读者能分辨哪句是事实、哪句是你的主张**。

- 事实层用陈述：「8 条反馈里 5 条是同一类问题」
- 判断层用主张：「我认为重点应该从『看得见』转向『用得上』」
- ⛔ 不许把判断伪装成事实：「数据表明应该优先做 X」——数据不会"表明应该"，那是你的推论

**③ 一次最多 3 条变化。** 与 §二·二 同源：超过三条，判断之间会互相稀释。
**今天只有一件重要的事，就只写一条——那是好日报，不是偷懒。**
硬凑出来的第四条通常是「开了个会」「调了个字段」，它一进来，整篇就从「帮人做判断」退回「汇报劳动」。

### 两个实操要点

**最难写的是第二层，不是第四层。** 大家以为难在下判断，其实难在说清为什么值得看——
因为「重要性」要求你知道读者关心什么，**这是材料里没有的外部知识**。
事实是抄的，影响和判断是推的，只有重要性靠你了解读者。

**最容易错的是二三层写成同一件事。** 区分判据：
**「为什么重要」是对这件事所在领域的意义，客观；「对业务的影响」是对读者的后果，关系性。**
同一件事对不同读者，影响不同，重要性相同。

### 改前 / 改后

> ❌ **改前**：本周区域试用 23 人，收到 8 条反馈，修复 3 个 Bug，下周继续优化。

四条并列、零解读，读者看完不知道产品到底怎么样。

> ✅ **改后**：
>
> **本周关键变化：使用起来了，但矛盾已经从「有没有工具」转向「能不能真正用起来」。**
>
> 累计 23 人使用，其中 17 人用了标签筛选——筛选占比七成，说明它已经是实际入口，不是顺带
> 点开的功能。下一步该缩短「筛选 → 找对象 → 触达」的路径，而不是继续加字段。
>
> 8 条反馈里 5 条是「找到之后不知道下一步做什么」——问题不在数据不够，在于产品没有形成
> 完整动作。这条比功能进度更值得排期。

两条变化，各自走完四层，第三条（修 Bug）压成一句交代。完整版见 `examples/03-罗列工作-改成-数字带解读.md`。

### 它不是新东西

§一.3「数字带解读」要求每个关键数字后面跟一句"这说明什么"——**那就是 F 后面接 I 的微缩版**。
本节只是把这条原则从句子级放大到条目级。所以它和现有规则是同源的，不是外挂的第二套标准。

---

## 三、去术语（第一优先级）——但有下限，不许滑到口语

> ⚠️ **本节曾因缺少下限而致返工**：照着“去术语、说人话”一路执行，写出了「同一批数据换个主角，就是能直接拿去谈的工具」这类句子，被判「太低端，失去中台的专业性」。**去术语 ≠ 说大白话。**

黑话与大白话是**同一个病的两端**，根因都是**没想清楚这句话到底在说什么事**：

| | 往上飘（黑话） | 往下滑（大白话） |
|---|---|---|
| 长这样 | 赋能引擎 / 抓手 / 护城河 / 全域闭环 / 降维打击 | 拿去谈 / 对不上账 / 动得了 / 收得回来 / 被骗了 |
| 自我感觉 | 显得专业 | 显得接地气 |
| 实际后果 | 听不懂，空 | 不庄重，把事说小了 |

**中间那条路 = 具体 + 准确，不是“简单”。**

**正文（老板层）零技术术语；折叠区/附录术语首现带一句人话。判据 = 外行可读性测试：非本团队业务同学通读正文无卡顿。**

### 唯一的检验动作：画面感测试

**读一句话，问：句子里的每个名词，听的人脑子里能不能浮现出具体的东西？**

| 原句 | 判定 |
|---|---|
| 区域竞争态势分析 | ✅ 一张图，上面是点位和它们之间的竞争关系 |
| 商业智能赋能平台 | ❌ 脑子里是空的 |
| 把做过一次的事变成能反复用的能力 | ❌「做法」「能力」「沉淀」都是空词 |
| 选点有可复现的规则，竞争有看得见的地图 | ✅ 规则、地图都是能指出来的东西 |
| 空白点位清单 / 价格异动清单 | ✅ 是名单，能打开看 |
| 该覆盖但还没覆盖的点位 | ❌ 说明性短语当专业名词用，散 |

**浮现不出来时，修法不是换个词，是回去想清楚这句话到底在说什么事。**

### 口语化黑名单（出现即改）

动词类：拿去谈 / 跑起来 / 收得回来 / 发得出去 / 动得了 / 打不进 / 对不上账
判断类：没人说得清 / 被骗了 / 讲不出 / 看不懂 / 心里没底
比喻类：发身份证 / 换个主角 / 蛋糕大 / 好钢用在刀刃上 / 拍脑袋 / 抢客
语气类：定死 / 省事 / 不夸大 / 其实 / 说白了 / 就是

术语→人话对照表（按你所在团队的技术栈自行增补）：

| 术语 | 人话 |
|---|---|
| 灰度 / 灰度名单 / dev 门 | 先对白名单内的同学开放试用 |
| 数据仓库 / 数据库直连 / 取数内核 | 直接读真实数据 |
| 合入主干 / 合 master / 回绿 | 进入正式代码库 / 恢复正常 |
| 下推 / 并发 / 缓存 | 把重计算挪到数据库里做（或删过程只留结果） |
| BRD / PRD / DRD | 三层设计文档（业务 / 产品 / 技术） |
| 配置中心 / 配置回退 | 随时可切回旧方式 |
| 口径 / 周环比 / pp / mock | 统计标准 / 比上周同期 / 个百分点 / 模拟数据 |
| 单点登录 / 权限域 / 权限钳制 | 登录与权限控制 / 只能看到自己负责的范围 |

**技术过程细节的默认处理是删，不是翻译**：读者要结果和意义，不要实现路径。

## 三·五、从"没毛病"到"有业务感"（守完规则仍然生硬，看这一节）

> 起因：严格执行完前面所有规则后，材料**没有错也没有特点**，还要再交给模型润色一遍才像人写的。
> 这不是执行不到位，是**设计后果**——本文档禁止类的规则有五十余条，许可类只有两条。
> 两头都压（不许飘成黑话、不许滑成口语），压到中间。中间是安全区，也是无聊区。

### 生硬的真正原因：黑话和大白话缺的是同一样东西

| | 例子 | 缺什么 |
|---|---|---|
| 黑话 | 赋能区域、构建闭环、能力建设 | 不指向任何具体业务事实 |
| 大白话 | 拿去谈、跑起来、动得了 | 是通用口语，不带业务信息 |
| **有业务感** | 以前拜访客户前要翻三个系统查周边有几家竞对，现在打开就能看 | —— |

**两头都不是"太专业"或"太通俗"的问题，是句子里没有业务。**
去术语只解决了前一头，说人话只解决了后一头，中间那个空缺谁都没填。

### 业务感 = 三样东西的密度

| 要素 | 是什么 | 例 |
|---|---|---|
| **业务对象** | 这个业务里真实存在的东西 | 名单、地图、点位、客户、策略编号 |
| **业务动作** | 这个业务里真实发生的操作 | 圈选、导出、拜访、对账、下发 |
| **业务场景** | 一个具体的时刻或情境 | 拜访前、月底结算时、换个城市重跑时 |

**判据**：一句话有业务感，当且仅当它至少包含其中一样。
三样都没有的句子，无论写得多"专业"或多"通俗"，读者脑子里都是空的。

⭐ **三样里最被低估的是场景。** 生动感最大的来源不是形容词，是**一个具体时刻**——
「以前拜访客户时很难回答周边有哪些竞争对手」之所以有画面，是因为它把读者放进了那个时刻。
`language-packs/` 里请把常用场景登记成 `type: scenario`，它和产品名一样是你的业务资产。

### 许可清单：允许你这样活（配额制）

前面的规则是下限，这里是上限。**给配额而不是给原则**——原则挡不住"为了保险什么都不敢写"。

| 允许 | 配额 | 为什么要有配额 |
|---|---|---|
| **口语化的真实表达** | 全文 1–2 处，放在最需要共情的地方 | 一处没有 = 报告体；不限量 = 滑向大白话 |
| **长短句交替** | 一段里句子不要全落在同一长度区间 | §四 只管上限 40 字，是单向压缩；压完全是短句，一样生硬 |
| **具体到能想象的细节** | 每个关键判断至少配一个 | 「效率低」是抽象，「要开三个表格来回对」是画面 |
| **明确的人称** | 关键动作句必须有主语 | 报告体的默认是无主语，这是生硬最大的单一来源 |

### 写出活人感的六条

- 用真实任务和动作承载情绪，不用口头禅制造亲切。
- 介绍工具时写"以前怎样完成 → 现在怎样完成 → 少做什么、多看见什么"。
- 允许有长有短、有轻有重；重要场景展开，承接内容一句带过。
- 一条事实最多补一层距离业务最近的解读。
- 标题和核心主张使用已有业务语言；普通转场和叙事表达保留创作自由。
- 发现同一金句反复出现时，回到原始业务场景重新表达。

### 机器能帮到哪一步

`business_language.py lint` 会报**业务感密度过低**（整段没有对象、动作、场景中的任何一样），
但它只能告诉你"这段是空的"，**说不出该填什么**——该填什么只有你知道，因为它取决于你的业务。

所以这一节和别的章不同：**其余章节机器能判对错，这一节机器只能提示缺失。**
真正让它长进的是 `cases/` —— 每次润色后把"改前 → 改后"记一条，
攒够几十条，你就有了一份从自己材料里长出来的活人感语料，比任何静态清单都准。

## 四、短句化

- 一句 **>40 字**或 **≥3 个顿号/加号并列** → 必拆（拆子弹或断两句）。
- 一个子弹只说一件事；破折号、括号补充每句各最多一处；括号里再套并列 = 必拆。
- 工具：`python3 tools/style-lint.py <md>` 扫句长/并列/术语命中。

## 五、保留清单（去术语和压缩时不许砍）

数字+解读、「不是A而是B」、判断句开头、逻辑因果链（现状→问题→动作→效果）、三行结构（干了什么/效果/下一步）。
**任何“简洁化”要求与八条方法冲突时，以八条为准**（例：卡片式样例几乎无数字、平铺无转折——不学）。

## 六、三个轻改件（进展类条目通用）

1. **状态徽标前置**：已上线/已完成/进行中（进行中内嵌时间承诺，如“计划周五前产出”）。
2. **业务价值的问法**：是什么的前提、谁因此能干什么、为后续什么铺路。
3. **交付物名词高亮**（品牌色文字非实底），让读者先看到“交付了什么东西”。

## 七、表达红线（违反=返工）

- **直白禁黑话**：禁抽象比喻（弹药就位/按图索骥/载体）——每个能力直说它解决什么问题。
- **客观禁拉踩**：旧方法只陈述事实局限，禁“碰运气/裸奔/头头是道数字对不上”式嘲讽。
- **SMART 是检查表不是句式**：动因/产出物/结果/价值四要素齐，但语义转写成自然叙事，禁“因为…产出了…结果是…”填空。
- **组织用词习惯优先于本规范**：不少组织对个别词有自己的用法约定，涨跌配色、金额单位也常有本地惯例。落地前先确认所在团队的用词表，并同步更新 `tools/rewrite-check.py` 的禁词表。

## 七·五、事实纪律（写错一条就毁掉整份材料的可信度）

> 起因：一份材料里出现「3 人 · 产品/研发/数据」——**团队构成是模型臆造的**，委托方从没说过。
> 对方的反应不是纠错，是要机制：「**我也不可能一一能识别你的对错的**」。

**1 · 四类信息，只有前三类能写**

| 类型 | 判据 | 处理 |
|---|---|---|
| `runnable` | 能跑命令当场复现 | 附复现命令 |
| `document` | 有文档出处 | 注明文件+位置 |
| `owner` | **只有委托方知道** | **绝不自行填写**，必须有确认记录 |
| `unverified` | 没核过 | **不许进正文** |

**`owner` 类信息清单（永远不许自填）**：团队构成与人员分工 · 业绩目标绝对值 · 考核占比 · 组织架构与汇报关系 · 任何“某某说过”的转述 · 未经实跑或文档佐证的比例与倍数。

**这类信息的危险在于：它读起来毫无破绽，委托方不特意查就发现不了。**

**2 · 相关性不许说成因果**

数据显示 A 组转化率高于 B 组 ≠ 用 A 规则能提高转化率。可能是“本来就优先做了 A 组”。
写法：标题用「数据显示：具备这三项特征的，历史转化率明显更高」，**不写「选对了成交率高五成」**。
并在同一份材料里显式标注结论性质，不能前面讲因果、后面在边界里承认是相关性。

**3 · 不夸大当前状态**：快照就说快照，验证过一次就说一次，演示件不叫产品。

**4 · 落地机制**：数字多的材料建事实台账（YAML）+ 校验脚本，材料里每个数字必须在台账登记，否则校验失败。

## 七·六、全局一致性检查（成稿后必做）

一份材料里同一件事出现两种说法 = 当场被抓。**这三类矛盾最常见**：

| 矛盾类型 | 实例 | 修法 |
|---|---|---|
| **进展 vs 堵点** | 开篇「作战产品备到位」，结尾「接口不能写、链路断在这」 | **不是删掉堵点，是把“就绪”收敛到准确范围**：「数据洞察与名单圈选已具备生产条件，任务下发接口联调后全线运转」 |
| **结论 vs 边界** | 标题讲因果，边界里承认是相关性 | 统一到较弱的那个表述 |
| **主张 vs 实态** | 主标题讲“可反复使用的能力”，正文承认是“快照演示件” | 主张必须覆盖得住实态 |

**检查动作**：成稿后从头读一遍，凡出现“已完成/已就绪/已具备”，去全文搜同一件事有没有“未打通/待接入/暂不可用”。

### 还有三类，容易漏

**四 · 连锁事实风险**：材料里某条事实，会不会动摇另一条结论？

> 实例：材料里写「主数据表的主键改为稳定标识，同一对象不再关联失败」——意味着**此前主键会漂**。
> 而同期材料里那条跨两期关联算出的转化基准线，正是靠这个标识关联的。**一条修复记录，反过来质疑了另一章的核心数字。**
> 处理：不是删掉任一条，是**当场做证伪**（实测受影响样本仅万分之二，且漂移只会导致低估、不会高估），然后在材料里把证伪结论写进口径说明。

**检查动作**：材料里每条“修复了什么”“改进了什么”，问一句——**这说明在此之前它是坏的，那依赖它的结论受不受影响？**

**五 · 结构分量错配**：重要的东西被放在次要位置。

> 实例：「线下名单可直接导入系统」被归入“配套能力”，但它打通的是外部数据进系统这一核心堵点，与“任务下发”构成一进一出的完整动作。
> 处理：分量按**它解决的问题有多核心**排，不按开发工作量排。

**六 · 职责声明与实际动作矛盾**：声明“这事不归我管”，材料别处却有相关动作。

> 检查动作：凡写了“不在我方职权范围”“由某方承接”，去全文搜有没有自己做了同一件事的记录。有 = 要么删声明，要么说清楚边界在哪一刀。

## 七·六·二、全局冗余检查：同一件事说三遍，也要单独查

§七·六检查的是**同一件事出现两种说法**，防止前后矛盾；本节检查的是**同一件事以相同或相近的话反复出现**，防止篇幅被重复占用。两者不能互相替代：前者问"说法是否一致"，后者问"每次再说是否都有必要"。

肉眼通读不足以完成这项检查。重复常分散在不同章节，读到后文时已经记不清前文的原句，必须先机扫定位，再逐处判断。

> **⚠️ 最容易漏的一条：紧邻两句承担同一职责 = 纯重复，跨章有职责也不豁免。**
> 章首判断句与紧跟其下的卡片判断句、条目标题与紧跟的第一条子弹，
> 这两处是复印高发地 —— 它们贴得太近，"各有职责"讲不通。

### 判据：先看职责，再看增量

同一事实再次出现时，逐处回答三个问题：

1. **章节职责是否不同**：这一次是否在回答本章独有的问题，如"本月到哪了""这是什么""目标是否健康"？能说清独立职责，才进入下一问。
2. **是否提供必要增量**：相对前一次，是否增加了本章必需的状态、解释或口径？换个说法、换个标题、换个位置，都不算增量。
3. **删掉是否损害本章理解**：删掉后，本章的结论、证据链或独立阅读是否断裂？不受影响，就是纯重复；受影响，才是有职责的复述。

判定规则：**职责不同 + 有必要增量 + 删除会损害本章理解 = 有职责的复述，可以保留；任一处只是复述前文、删除后信息无损 = 纯重复，应删或合并。**

### 三类典型冗余

| 类型 | 识别信号 | 处理 |
|---|---|---|
| **原句复印** | 章首判断句与下方卡片判断句逐字相同 | 保留承担主判断的一处，另一处直接删，不改写成近义句 |
| **标题与正文互抄** | 条目标题已写明数量、状态或时间，紧跟的子弹再次完整复述 | 子弹只留新增的解释、影响或下一步；没有增量就删 |
| **跨章近义复述** | 两章判断句主语、结论和证据相同，只换少量词；相似度高但各章职责说不清 | 明确主归属；其余章节**要么删除，要么压成一句不复述结论与数字的指向**（如「见本月进度」），不做近义改写 |

### 工具：先定位，不代替判断

```bash
python3 tools/dup-scan.py <材料.md>
```

输出三类线索：**逐字重复的整行 / 高频实体短语 / 相似句子对**。先看整行重复和高相似句，再回到上下文做"职责—增量—删除影响"三问；词频与相似度只提示复核，**不能直接作为删除依据**。

**一份周报的实测**：某产品名全文出现 13 次；同一条上线状态说明重复 3 处；同一个进度数字重复 3 处；两章判断句相似度 0.88，几乎逐字相同；机扫报出的相似句对共 51 对，按建议精简后降到 44 对。**肉眼通读一处都没发现**，机扫才把它们集中暴露出来。

### 两类噪音，禁止机械清理

- **设计要求的同名小节**：如两章都有「主线一 · XXX」，属于刻意的结构对称；标题相同不等于内容重复。
- **结构性相似行**：表格行、能力标签行常因格式一致被报为相似；先看字段承载的事实是否不同，不因句式相同删除。

### 不许删的东西

- **有职责的复述**：同一事实分别承担进度、定义、目标对账等不同职责，且每处对本章理解不可缺。
- **口径与审计锚点**：同一数字在总览、表格口径、文末归档审计中各有定位作用。
- **贯穿全文的正式名与主框架**：产品名、项目名、目标名称这类。高频不等于冗余，**不得为降低词频擅自换同义词**。
- §五 保留清单里的各项；精简不能拆断证据与结论。

**性能**：4 万字材料约 1 秒（内部用 difflib 的快速上界做了剪枝）。
`--ratio` 调相似度阈值（默认 0.55），`--min-times` 调实体短语的最少出现次数（默认 3）。

## 七·六·五、视觉标准（HTML 汇报材料）

材料长什么样，看 **`visual/VISUAL-STANDARD.md`**——两套配色标准（暖色 / 清新）+ 八条规格 + 两个可直接复制的模板。

⛔ **第一铁律：不用黑框压顶**。首屏 hero、结尾 ask 一律不用纯黑满版底，
那是压迫感的来源。改浅色底 + 暖调墨色文字，品牌色只留给强调处。

⚠️ 改造既有材料时，`.hero` 这类选择器在这套 CSS 里有 4–5 条规则（旧规则 + 追加覆盖块），
**grep 到的第一条永远是旧的纯黑，必须取最后一条**。踩过：文案已换新版、视觉还是黑 hero。

起步直接复制模板：`visual/template-warm.html`（季度规划默认）或 `template-fresh.html`（项目型汇报）。
两个模板的正文均为虚构示例，替换正文即可使用；品牌色改 CSS 顶部 `--orange-*` 一组。

## 七·七、模型协同润色（三轮实测定型）

> 起因：自己写的稿评审只给 60 分，诊断是**只做减法不做加法**——口语去干净了，但八条方法一条没落实。
> 结论：**润色环节必须上模型**，但要管住它。三轮下来 60 → 70 → 80 分。

### 分工（不可颠倒）

| 环节 | 谁 | 判据 |
|---|---|---|
| 起草 | **自己** | 事实、篇幅、结构 |
| 精进 | **模型** | SMART 四要素 + 表达强化（标题即观点 / 不是A而是B / 数字带解读） |
| 事实验收 | **代码** `tools/rewrite-check.py` | 数字增删、禁词、口语、篇幅、超长条目 |
| **解读验收** | **自己** | ⚠️ 代码验不了，见下 |
| 定分 | 委托方 | — |

**为什么润色要交给模型**：自己写会求稳——怕编造就不敢加解读，怕啰嗦就不敢展开，写出的是“不犯错的 60 分稿”。模型没有这个包袱，敢往前推一步；**其中一部分越界，另一部分正是自己不敢写的那句**。价值在这里，不在“改得对”。

### ⚠️ 代码验收的盲区（最贵的一条）

`rewrite-check.py` 能验数字**有没有变**，验不了数字**被解读得对不对**。

> 实例：模型把「14 个模块中 9 个已进生产」写成标题「7 月交付**近半数**模块进生产」。
> 9/14 = 64.3%，是「多数」不是「近半数」。**校验器全绿通过**——数字没增没减，但语义错了。

**人工必检**：所有量化描述词（近半数 / 多数 / 大部分 / 超过一半 / 全部 / 基本），回原文算一遍。

### 模型选型：认版本，不认品牌

同一品牌的不同版本，中文写作水平能差 25 分。上一代轻量版容易写出公文腔（圆满 / 顺利 / 成果显著 / 革新），并且会丢掉「不是A而是B」这类结构。

**教训**：曾因限流回退到同品牌的轻量旧版，据此下了“某品牌不行”的结论——**错的**。
**做法**：把候选模型当版本管理，每换一版重跑一次同一篇基准稿并记录得分；不要凭品牌印象选型。

**另有一类模型不适合润色**：跨条目关联推理强、擅长找矛盾，但会自行补因果、把篇幅撑大。这类留给“一致性检查”环节用，不进润色链路。

### ⚠️ 约束强度决定模型敢改多少

同一篇材料、同一个 prompt，A 模型只改 3 处、B 模型改 9 处。差异不是能力，是 **prompt 里堆了 7 条硬约束 + 两张禁词表，保守的那个选择了不冒险**。

**取舍**：约束写死（保事实、保产品名）→ 拿到安全但保守的稿；约束放松 → 拿到大胆但要逐条核的稿。
**做法**：约束照写不放松（编造的代价更大），但**必须同时跑两个模型**——总有一个敢改。
只跑一个模型且它恰好保守，等于白跑一轮。

**模型改对的地方高度集中在一类：去技术词与去名词化**（沉淀→定 / 系统化→走系统下发 /
链路→流程 / 落库→存下来 / 回填→填回 / 对齐投放→跟着投放）。这类词自己写时察觉不到，
因为在技术语境里它们就是常用词——**这正是模型润色不可替代的地方**。

### 调用参数（踩空返回的坑）

**`max_tokens` 必须给足——推理模型会把额度全吃在思维链上，正文返回空**：

| 模型类型 | max_tokens | 实测 |
|---|---|---|
| 长思维链推理模型 | **≥ 65536** | 给 20000 时思维链近 3 万字符、**正文 0 字符**；给 65536 才出稿 |
| 常规对话模型 | 16000 够 | 思维链 6-11k 字符 |

模型名大小写、内外网端点差异也会导致 4xx。接入新模型时先用一句话 prompt 探通，再跑长文。

### 模型的四类惯犯（prompt 里必须逐条堵）

| 类型 | 实例 | 严重度 |
|---|---|---|
| **编造事实** | 「因排期资源冲突顺延」「存在数据孤岛隔离」——原文都没有 | 🔴 最高红线 |
| **解读越界** | 「远非单点修补」「至此可用」「已按期完成」——材料推不出的价值判断 | 🟠 需逐条核 |
| **量化词错误** | 9/14 说成「近半数」 | 🟠 代码抓不到 |
| **擅自改结构** | 加二级缩进来压句长（违反“结构不动”，也踩周报禁三层缩进） | 🟡 退回 |

**解读的界限**（写进 prompt 的原话）：
- ✅ 留：「14 个模块中 9 个已进生产 —— 多数已落到线上」（9 和 14 都在材料里）
- ❌ 删：「远非单点修补」（“单点修补”是外部对照物，材料里没有）

### 用法

```bash
# 1. 自己起草 → draft.md
# 2. 润色（prompt 模板见 tools/smart-polish-prompt.md，末尾拼上 draft.md）
# 3. 验收
python3 tools/rewrite-check.py <基准原文> <候选1> [候选2 ...]
# 4. 人工核量化词 + 越界解读 → 取长补短出终版

# 5. 沉淀 —— 这一步不做，下次润色还是从零开始
python3 tools/business_language.py extract-cases draft.md final.md --output cases-new.yaml
```

**第 5 步是这套流程里唯一能积累的一环。** 它从「原稿 vs 终版」自动找出被改写的段落对，
产出 case 候选；`reason` / `audience` / `material_type` 三项**留空待你补**——
为什么这么改是判断，diff 产不出。补完并进 `cases/*.yaml`，下次写作时它会被读到。

> 攒够几十条，你就有了一份从自己材料里长出来的活人感语料（见 §三·五）。
> 静态清单说不清"什么叫生动"，案例能。

**盲测建议**：两个模型跑同一 prompt，产物用中性代号落盘（cand-1 / cand-2），映射另存，**评完再揭晓**——避免被品牌先入为主。

## 八、成文 SOP（七步）

1. **锁定改写边界**：确认听众、场合、时长和用户真正要改什么。先列“原样保留 / 局部调整 / 移动删除”三类清单；用户只要求结构优化时，不扩大成全篇换文风。
2. **抽要素**：每条列 A 动因 / B 产出物 / C 结果 / D 价值（工作台，不进稿）。
3. **选骨架 + 重排结构**：先按 §二 判断这份材料属于哪一种——介绍一件事（§二·五 四段论证）/ 报告变化（§二·六 F-I-I-J）/ 复盘（业务闭环五步）；再按听众问题安排详略。结构可以调整，成熟句子随内容移动，不因换章节而重写。
4. **成文**：判断句开头 → 短句支撑（数字带解读）→ 价值/下一步。先写业务场景，再写名单、图、规则或工具，最后写怎么使用。
5. **模型精进**（§七·七）：只把“局部调整”内容交给模型；将“原样保留”句作为不可改锚点写入 prompt。`tools/smart-polish-prompt.md` 按其注明的周报场景调参；季度汇报等其他材料沿用其中事实和听感红线，不套用其固定篇幅与结构。模型不得为了统一风格重写成熟句子。
6. **机检**：运行 `tools/rewrite-check.py`、`tools/style-lint.py`、`tools/dup-scan.py`（跨章冗余）和事实台账校验。机检只证明格式与事实纪律，不证明听感。
7. **人检**：
   - 逐句做原文对照，新句没有更准确、更清楚、更温和就恢复原句
   - 站在听众位置做归责检查：问题对事，不对人
   - 朗读全文，检查是否自然、专业、能直接讲出口
   - 量化词逐个回原文计算，逐条核模型新增解读
   - 按 §七·六扫描全局矛盾，按 §五确认关键判断没有被压缩掉

## 九、业务语言学习内核（让这份规范自己长大）

前面八章是**写法**，这一章是**机制**：让每一次「这句不行、改成那样」都被记下来，
下次自动提示，而不是靠人记住。核心命令 `tools/business_language.py`，只依赖 Python 标准库 + PyYAML。

### 它管什么、不管什么

| 管 | 不管 |
|---|---|
| 记住哪些表达被采纳、哪些被否定，以及**为什么、对谁说** | 替你决定文案——所有检查都只提示，不阻断、不自动改写 |
| 保护产品正式名不被禁词规则误删 | 联网抓资料——**全包零联网代码**，采集器由你提供 |
| 把聊天里的高频说法收进候选区 | 把高频说法自动转成正式口径（必须人确认） |
| 生成匿名评测包，让多个模型独立评价同一稿 | 用模型的多数票定稿 |

### 四份可长大的资产

```
language-packs/common.yaml         通用写作语言（跨业务稳定成立的，别往里塞业务词）
language-packs/example-domain.yaml 业务词包模板 —— 复制成你自己的域
profiles/example.yaml              作者偏好档案 —— 随反馈逐条长出来
cases/example.yaml                 正反案例库 —— 比词表有用，它保留了「为什么」
```

**状态流转是这套东西的命门**：

```
candidate → supported → pending_confirmation → approved
```

只有 owner 确认、已采纳稿或正式命名能直接写 `approved`。
**聊天里说得最多的词，往往正是最没被推敲过的词**——所以高频只提高证据等级，永远不自动转正。

### 七个命令

```bash
# 自动选词包（看它命中了哪些触发词）
python3 tools/business_language.py route draft.md

# 影子检查：按标题/主张/总结/正文分级提示，只提示不阻断
python3 tools/business_language.py lint draft.md --report report.json

# 检索已确认的知识卡（写之前先查，别凭印象补词）
python3 tools/business_language.py knowledge-query "主题词"

# 检索写作技巧（这个场景该用什么手法）
python3 tools/business_language.py technique-query "标题怎么写"

# 记录一次反馈事件 —— 这一步不做，前面全白干
python3 tools/business_language.py learn \
  --before "共同承诺" --after "一起把这件事做成" \
  --reason "对合作团队有压力" --audience region --material-type speech

# 把采集到的 JSONL 抽成候选表达（kind=external 时物理隔离，禁止转正）
python3 tools/business_language.py ingest --input corpus.jsonl --output candidates.yaml --kind dc

# 一致性脱敏：同一实体全文稳定映射，不删句不改结构
python3 tools/business_language.py sanitize draft.md --entity "某某团队" --number-scale 0.8

# 生成匿名评测包：三个角色拿同一组问题，评完再揭晓谁是谁
python3 tools/business_language.py blind-packet draft.md --entity "某某团队" --output packet.json
```

### 两个必须理解的设计

**1 · 专有名词优先于禁词表。** 词包里 `type: product` 且 `approved` 的名字会被"保护"——
即使名字里含通用禁词（如「赋能」），检查时也不会被报。
起因：模型见「赋能」在禁词表里，把主线产品正式名直接删成了另一个名字。
**把你自己名字里带禁词的产品名登记进去**，否则模型会擅自给你的产品改名。

**2 · 私密材料永不进版本库。** 聊天语料、脱敏映射、由个人对话推导出的知识卡，
全部只写 `.runtime/`（已在 `.gitignore` 里）；真实的群名与联系人写 `references/sources.local.yaml`（同样已排除）。

`tools/check_local_privacy.py` 是配套的 pre-commit 闸门，命中即阻断提交：

```bash
# .git/hooks/pre-commit
python3 tools/check_local_privacy.py || exit 1
```

它拦四类：本地私密路径、看起来像聊天扫描产物的文件名、含 `content/source/speaker/timestamp` 结构的文件、标了 `privacy: local_only` 或个人评价标记的内容。

> ⚠️ **这个闸门只管 git 暂存区，管不了「打包整个目录发出去」。**
> 对外分发前请手动确认 `.runtime/` 与 `sources.local.yaml` 不在包里。

### 当前边界（别误解它的能力）

- **不联网**。设计上就不联网（详见 `references/DESIGN.md` §5）。`ingest` 的输入是你准备好的 JSONL。
- **外部资料学的是写法，不是业务词**。`--kind external` 走独立的技巧定位器
  （`techniques/`），认对照结构（不是A而是B / 以前…现在 / 数字带解读 / 破误解）。
  ⚠️ **它只定位候选段落，不生成技巧**——`problem` / `technique` / `avoid_when`
  三个字段是判断，正则产不出，留空待人或模型补全。外部来源永远 `candidate` + `promotion: forbidden`。
- **知识检索是关键词匹配**，不是语义检索。没命中会明确返回未命中，不会编。

完整设计与验收标准见 [`references/DESIGN.md`](references/DESIGN.md)。

## 包内文件

| 文件 | 作用 |
|---|---|
| `SKILL.md` | 本文——写什么、怎么写 |
| `tools/style-lint.py` | 机检句长、并列过载、术语命中、归责表达 |
| `tools/rewrite-check.py` | 改写稿验收：数字增删、禁词、口语、篇幅、SMART 覆盖 |
| `tools/business_language.py` | 学习内核：路由 / 影子检查 / **业务感密度** / 反馈 / **case 抽取** / 采集 / 检索 / 脱敏 / 盲测 |
| `tools/dup-scan.py` | 跨章冗余扫描：逐字重复整行 / 高频实体短语 / 相似句子对 |
| `tools/check_local_privacy.py` | pre-commit 闸门：拦住聊天语料与个人评价进版本库 |
| `tools/smart-polish-prompt.md` | 交给模型润色时的 prompt 模板 |
| `language-packs/*.yaml` | 通用写作语言包 + 业务词包模板 |
| `profiles/example.yaml` | 作者偏好档案模板 |
| `cases/example.yaml` | 正反案例库模板 |
| `techniques/example.yaml` | 写作技巧库：每条必须有 before/after 对照与 avoid_when |
| `references/DESIGN.md` | 学习内核的完整需求、边界与验收标准 |
| `references/sources.example.yaml` | 采集来源配置模板 |
| `tests/` | 27 个单测，覆盖路由、检查、脱敏、反馈、技巧抽取与隐私闸门 |
| `visual/VISUAL-STANDARD.md` | HTML 汇报材料的八条视觉规格 |
| `visual/template-warm.html` | 暖色版模板（季度规划默认） |
| `visual/template-fresh.html` | 清新版模板（项目型汇报） |

**装完不需要配置任何东西**：本文的写作规范是纯文档，装好即生效；
`style-lint.py` / `rewrite-check.py` / `check_local_privacy.py` 三个脚本零第三方依赖，
直接可跑。学习内核需要 PyYAML，缺了会提示而不是报错。

**用过两三次、有具体不满意之后**，再回来做这三件（不做也能用，做了它才认识你的业务）：
① 把你的产品正式名登记进 `language-packs/`，尤其是**名字里带通用禁词的**（否则模型会给它改名）；
② 把团队黑话补进 `style-lint.py` 的 `TERMS`——表里没有的自研系统名、项目代号才是最该加的；
③ 复制 `profiles/example.yaml`，每次改稿后 `learn` 一条，让它记住你的偏好。

装上 pre-commit 闸门（如果你会往里存聊天语料，这一步必做）：

```bash
echo 'python3 tools/check_local_privacy.py || exit 1' >> .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit
```
